1. **Global Discount**: Use `%` and `Fixed` grid just like POS.
2. **Item Wise**: Instead of Pencil icon opening modal, show inline `SmartNumberInput` for `%` and `Amt` on each hospital bill. Use a small local state wrapper to handle `useUpdateSale` seamlessly.
